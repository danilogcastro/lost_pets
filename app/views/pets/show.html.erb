<h1><%= @pet.name %></h1>
<h2><%= @pet.location %></h2>
<p><%= @pet.species %></p>
<p><%= @pet.found_on %></p>

<%= link_to 'Update this pet', edit_pet_path(@pet) %>

<%= link_to 'Delete this pet', pet_path(@pet), method: :delete, data: { confirm: "Are you sure?"} %>